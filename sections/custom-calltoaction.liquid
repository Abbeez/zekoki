<style>
       .gradient-section {
         position:relative;
         width: 100%;
         height: 400px;
         background-size: cover;
         color: white;
         padding: 20px;
         overflow:hidden;
       }

  .gradient-section-img {
        position:absolute;
  }

       /* .gradient-section:after{
             content: "";
         position: absolute;
         top: 0;
         left: 0;
         width: 100%;
         height: 100%;
         background: rgb(218,41,28);
         background: linear-gradient(141deg, rgba(218,41,28,0.938813025210084) 0%, rgba(46,34,33,0.9360119047619048) 67%);
         z-index: 1;
       }
  */

       .content {
         position: relative;
         z-index: 3;
         max-width: 800px;
       }
</style>

<div
  class="gradient-section"
>
  <div class="gradient-section-img">
    <img src="{{ section.settings.gradient-background | image_url }}');" height="" width="">
  </div>

  <div class="content">
    <h2>{{ section.settings.heading }}</h2>
    <div class="subtext">{{ section.settings.subtext }}</div>
  </div>
</div>

{% schema %}
{
  "name": "Gradient Section",
  "settings": [
    {
      "type": "image_picker",
      "id": "gradient-background",
      "label": "Background Image"
    },
    {
      "type": "color",
      "id": "gradient_start",
      "label": "Gradient Start Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "gradient_end",
      "label": "Gradient End Color",
      "default": "#000000"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Your Heading Here"
    },
    {
      "type": "richtext",
      "id": "subtext",
      "label": "Subtext",
      "default": "<p>Your subtext here.</p>"
    }
  ],
  "presets": [
    {
      "name": "Gradient Background"
    }
  ]
}
{% endschema %}
