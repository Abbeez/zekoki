{%- style -%}
      .custom-servicecenter__header {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: center;
        gap: 20px;
    }

    .custom-servicecenter__body {
          display: flex;
          gap: 20px;
          justify-content: center;
          align-items: center;
          flex-wrap: wrap;
      }

      .custom-servicecenter__items {
        flex: 1 0 100%;
        text-align:center;
        /* min-wheight:500px; */
      }

      .custom-servicecenter__items_wrapper{
      position: relative;
      padding: 1rem;
      background: var(--zekoki-primary);
      border-radius: 1rem;
    }

      .custom-servicecenter__items_wrapper h2 {
        background: var(--zekoki-primary);
        color: var(--zekoki-white);
        padding: 20px;
        width:100%;
        margin: 0 !important;
    }

    /* Tablet and small desktop */
  @media screen and (min-width: 750px) {

      .custom-servicecenter__items {
        flex: 1 0 45%;
      }
  }

  /* Large desktops */
  @media screen and (min-width: 990px) {

  }
{%- endstyle -%}

<section
  class="page-width custom-servicecenter__section scroll-trigger animate--slide-in"
  style="
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  "
>
  <div class="custom-servicecenter__container">
    <div class="custom-servicecenter__header">
      <div class="custom_servicecenter__header--container">
        <h2 class="h1 title">{{ section.settings['custom-servicecenter__title'] }}</h2>
      </div>
      <div class="custom_servicecenter__header--container">
        <p>{{ section.settings['custom-servicecenter__description'] }}</p>
      </div>
    </div>

    <div class="custom-servicecenter__body">
      {% for servicecenter in shop.metaobjects['service-centers'].values %}
        <div class="custom-servicecenter__items">
          <!--
            <div class="custom-servicecenter__items_wrapper">
              <h2>{{ servicecenter.title }}</h2>
            </div>
          -->
          <h2>{{ servicecenter.service_centers.service_center_name }}</h2>
          <a
            href="
              {% if page.handle == 'servicecenteres' %}
                  service-centers/{{ servicecenter.system.handle }}
              {% else %}
                 pages/service-centers/{{ servicecenter.system.handle }}
              {% endif %}
            "
            class="button button--primary"
          >
            Branch Details
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Custom Service Center",
  "settings": [
  {
    "type": "header",
    "content": "Section Spacing"
  },
  {
    "type": "range",
    "id": "padding_top",
    "label": "Padding Top",
    "min": 0,
    "max": 100,
    "step": 5,
    "default": 20
  },
  {
    "type": "range",
    "id": "padding_bottom",
    "label": "Padding Bottom",
    "min": 0,
    "max": 100,
    "step": 5,
    "default": 20
  },
  {
    "type": "header",
    "content": "service center Details"
  },
  {
    "type": "text",
    "id": "custom-service--center__title",
    "label": "servicecenter Title",
    "default": "Quality Products"
  },
  {
    "type": "textarea",
    "id": "custom-service--center__description",
    "label": "servicecenter Description",
    "default": "Product that you will truly love"
  },
  ],
  "presets": [
    {
      "name": "Custom Service Centers Section"
    }
  ]
}
{% endschema %}
